
/**
 * @brief Represents an returnable error code.
 *
 */
typedef struct {
    float x; /**< X coordinate */
    float y; /**< Y coordinate */
} Point;
enum error_code {
  ALLOCATION_FAILURE = -1,
};
